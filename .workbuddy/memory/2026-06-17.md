# 2026-06-17

## VSFA 接入 + 街机 GUI 升级

### 接入 VSFA 深度学习 NR 图像质量模型
- 用户通过微信发来 `VSFA_LIVE_best.pth` 权重文件和调用代码参考
- 模型架构：VGG16（ImageNet）→ GAP → Linear(512→512) → Linear(512→1)，输出 DMOS
- 新建 `vqa/algos/vsfa.py`（~260 行），懒加载 torch，带缓存
- 注册到 `ALGORITHMS` 表，key="VSFA", kind="NR"
- 测试三张样本图（首次下载 VGG16 权重 ~2.5 分钟）：ref=47.26 / dis_blur=62.70 / dis_noise=51.00
- 更新 README，git init + 首次提交

### GUI 街机风格升级
- 用户选择「街机方案」改造 GUI 视觉
- 新建 5 个组件：arcade_score / achievement / particles / typewriter_log / arcade_progress
- 重写 main_window.py 接入全部组件 + 成就追踪系统
- 离屏验证：截图正常 + 6/6 单测 PASS + CLI list 4 算法正常

### 街机四屏重构（完整的游戏化流程）
- 用户反馈"太一般了，要像真的玩游戏一样"，追加需求：主页→开始游戏→评估→结算 四屏流程
- 新建 7 个文件，替代旧 main_window.py：
  - `arcade_theme.py`：统一配色(霓虹蓝/粉/绿/黄) + 字体 + 评级系统 + 全局 QSS
  - `crt_overlay.py`：CRT 扫描线 + 渐变暗角覆盖层
  - `title_screen.py`：标题画面（Attract Mode）大霓虹标题 + 算法徽章 + 最高分 + "开始游戏"
  - `select_screen.py`：选关画面 FR/NR 双卡 + 算法标签 + 参数 + "开打！"
  - `game_screen.py`：评估画面 记分板 SCORE/RANK/帧数 + 双预览 + 霓虹进度条
  - `result_screen.py`：结算画面 "关卡通过！"+ 评级 + "再来一次""返回主页"
  - `arcade_main.py`：QStackedWidget 四页容器 + fade 淡入淡出切页动画 + 高分持久化
- 中英混用：算法名英文(SSIM/PSNR/VSFA)，界面文案中文(开始游戏/开打/关卡通过)
- 更新 gui/__init__.py 入口指向 ArcadeMain
- 离屏四屏全流程截图验证 + 6/6 单测 PASS
- Git 提交 (05ee7bf)

### 街机 v3 果汁升级
- 用户反馈"不够街机、不够爽"，要求真正游戏级的视觉冲击
- 下载 Press Start 2P 像素字体（118KB OFL），QFontDatabase 懒加载
- 新建 `starfield.py`：200 颗星星下移 + 闪烁背景（标题画面）
- 新建 `flash_overlay.py`：白色闪屏 150ms + "3-2-1-GO!" 像素大字倒计时
- 升级 `title_screen.py`：星空背景 + 像素字体标题 + 霓虹光晕 + "PRESS START" 呼吸闪烁
- 升级 `game_screen.py`：点击评估先倒计时再跑分；完成时窗口抖动
- 升级 `result_screen.py`：新高分检测 → "★ 新纪录！★" 金色条幅 + 三波粒子爆发
- 升级 `arcade_main.py`：闪屏切页 + 倒计时回调 + 屏幕抖三抖
- 6/6 单测 PASS，Git 提交 (e77dde6)
