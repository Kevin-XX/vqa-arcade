- 修复 VQA 街机版"页面切换出现强噪点"问题：定位根因为 `flash_overlay.py` 的 `_draw_glitch` 使用超高密度随机雪花绘制（`density ~ w*h*0.4`）。
- 将切页效果改为轻量过渡：`flash()` 帧数降为 3 帧，新增 `_glitch_alpha` 衰减；移除随机噪点，改为"短暂暗化 + 单条扫描线"。
- 保留倒计时逻辑不变（`countdown` 路径未改），并重启 `python -m vqa.gui` 验证进程正常启动。
- 应需求恢复 `starfield.py` 为原始 200 星版本（闪烁 + 下落）。
- **新增 VQA Web 版**：Flask 后端 + 单文件街机前端。后续多项修复：
  - API 改为绝对路径解决代理拦截上传失败
  - 新增静态资源路由解决字体 404
  - SSE 改用 EventSource + 心跳机制防假死
  - 修复 onScoreDone 中 segs 作用域 bug 导致结果不显示
  - stride/maxFrames 前后端贯通
  - 视频评估自动限帧（默认 60、上限 200）+ 帧数提示
  - 记录评分耗时 elapsed_sec
